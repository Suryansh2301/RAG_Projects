# --- UI ---
streamlit>=1.38.0

# --- LangChain core + ecosystem (post-1.0 split) ---
langchain>=1.3.14                # main package: agents/composability
langchain-core>=1.5.3            # base abstractions (Runnable, prompts, etc.)
langchain-classic>=1.0.8         # EnsembleRetriever now lives here post-1.0
langchain-community>=0.4.0       # PyPDFLoader, BM25Retriever
langchain-text-splitters>=0.4.0  # RecursiveCharacterTextSplitter
langchain-huggingface>=1.2.1     # HuggingFaceEndpoint, ChatHuggingFace, embeddings
langchain-chroma>=1.1.0          # dedicated Chroma integration package

# --- Hugging Face ---
huggingface_hub>=0.26.2

# --- Vector store (dense index) ---
chromadb>=0.5.20

# --- Sparse / lexical index ---
rank_bm25>=0.2.2

# --- PDF parsing ---
pypdf>=5.1.0

# --- Token counting ---
tiktoken>=0.8.0

# --- Env vars ---
python-dotenv>=1.0.1

#-------for embeddings ----
sentence-transformers>=5.6.1

torch>=2.13.0

langchain-google-genai>= 4.3.2